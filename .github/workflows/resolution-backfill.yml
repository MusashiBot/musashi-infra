name: Resolution Backfill

on:
  schedule:
    - cron: '15 * * * *'
  workflow_dispatch:

concurrency:
  group: resolution-backfill
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci
      - run: npm run preflight:env -- SUPABASE_URL SUPABASE_SERVICE_KEY
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      - run: npm run job:resolution-backfill
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          KALSHI_BASE_URL: ${{ secrets.KALSHI_BASE_URL }}
          RESOLUTION_BACKFILL_MAX_RUNS: '3'
          RESOLUTION_BACKFILL_MAX_DURATION_MS: '900000'
          RESOLUTION_BACKFILL_MAX_MARKETS: '400'
          RESOLUTION_BACKFILL_FETCH_CONCURRENCY: '3'
          RESOLUTION_BACKFILL_WORKER_RATE_LIMIT_MS: '200'
